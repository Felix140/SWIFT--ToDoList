import SwiftUI

struct DoneListView: View {
    var body: some View {
        Text("Done List")
    }
}

struct DoneListView_Preview: PreviewProvider {
    static var previews: some View {
        DoneListView()
    }
}
